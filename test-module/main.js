const utils = require("./utils.js");
// or can using destructuring const {sum, divide} = require("./utils.js");
const value1 = utils.sum(1, 2);
const value2 = utils.divide(1, 2);
console.log(value1);
console.log(value2);
